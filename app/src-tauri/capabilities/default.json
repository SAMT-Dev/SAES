{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main","loader"],
  "permissions": [
    "updater:default",
    "core:default",
    "opener:default",
    "core:app:allow-version","dialog:default"
  ]
}
