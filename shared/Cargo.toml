[package]
name = "saes-shared"
version = "1.0.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
sea-orm = { version = "1.1.11", features = [
    "sqlx-mysql",
    "runtime-tokio-native-tls",
    "macros",
] }
